import React from 'react';
/** Invalidate the cached NFT list for an address (or all addresses). */
export declare function invalidateExtensionNftCache(address?: string): void;
export declare const NFTsScreen: React.FC;
